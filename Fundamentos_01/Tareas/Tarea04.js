// TODO: Tarea #04: Aplicar todo lo que has visto en Fundamento I en una tienda electrónica. El tema de tu tienda puede ser cualquiera. Utiliza el módulo readline para gestionar los inputs del usuario.

const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});
